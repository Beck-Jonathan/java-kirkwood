import java.util.ArrayList;

public class factory {
    //public static release file(String name, String[] input) {
      //  String game=(String) input[0];
        //String release=(String) input[1];
        //String system= (String) (input[6]);
        //release name = release(game,  release_date, system);


    // return name;}
    }

